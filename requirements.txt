torch>=2.6
# Core libraries
pandas>=2.0.0
numpy>=1.24.0

# NLP and preprocessing
natasha>=1.6.0
pymorphy2>=0.9.1
razdel>=0.5.0

# Embeddings and vector search
sentence-transformers>=2.2.0
weaviate-client>=4.6.0

# BM25 search
rank-bm25>=0.2.2

# Cross-encoder for reranking
transformers>=4.30.0

# LLM as Judge (для GGUF моделей)
#llama-cpp-python>=0.2.0
# Для установки с GPU поддержкой (CUDA) или Metal см. инструкции в docs/server_setup_venv.md

# API клиент для LLM (опционально, если используешь API режим)
openai>=1.0.0  # для OpenRouter API (OpenRouter использует OpenAI-совместимый API)

# Utilities
tqdm>=4.65.0
scikit-learn>=1.3.0
huggingface-hub>=0.20.0  # для скачивания моделей

# Optional: for better text cleaning
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Git repository handling
GitPython>=3.1.40
pathspec>=0.11.0

# Configuration
PyYAML>=6.0.1
pydantic>=2.0.0

# Graph database
neo4j>=5.14.0

# Code parsing
tree-sitter>=0.20.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Telegram Bot
python-telegram-bot==20.7
requests>=2.31.0  # For Mermaid.ink API

# FastAPI REST API
fastapi>=0.109.0
uvicorn[standard]>=0.27.0  # ASGI server with auto-reload
python-multipart>=0.0.6  # For file uploads (if needed later)
